# Custom_Terminal_like_hacker
A fork of eDEX-UI with SSH remote monitoring support. Automatically switches system metrics (CPU, RAM, processes, network) to the remote Linux server when an SSH tunnel is active, while keeping the local Windows filesystem display. Built with Electron 13, Node.js 16, and a lightweight Python agent on the server side.
